# Визуальный мир: инженерный документ

Утверждён заказчиком 27.08.2026. Полная версия — `Projects/ecoconcept/website/design-system.md` в базе знаний.

## Тезис

Сайт читается как **технический документ хорошей инженерной фирмы**: плотный, с числами, таблицами и фотографиями своих объектов. Не как лендинг с иконками в кружочках. Весь «дизайн» держится на крупной типографике, числах как графике и чередовании светлых и тёмных секций.

Требование заказчика дословно: **«профессионально, а не мультяшно»**.

## Анти-референс — прежний сайт

Белая простыня: девять секций, семь по одному шаблону «kicker → заголовок → сетка карточек», восемь повторов одного призыва, цвет и вес только в футере. Всё это заменяется, а не полируется.

## Ритм: три типа секций

Каждая секция принадлежит одному типу. **Не более двух подряд одного типа.**

| Тип | Что | Фон |
|---|---|---|
| A — данные | Таблица, числа, расчёт | `--bg` белый |
| B — объяснение | Текст + схема или фото | `--bg-2` светлый |
| C — якорь | Крупная мысль, деньги, призыв | `--dark` тёмный |

**Якорей ровно три:** связка 60/40 · госкредит · заявка и футер. Это лечение белой простыни: вес распределён по странице, а не собран внизу.

## Палитра

Зелёный унаследован от бренда, исправлено его применение.

| Токен | HEX | Роль |
|---|---|---|
| `--bg` | `#ffffff` | Секции A |
| `--bg-2` | `#f5f6f4` | Секции B, шапки таблиц |
| `--ink` | `#16191c` | Текст и заголовки (15,9:1) |
| `--ink-2` | `#5b6560` | Вторичный текст (6,1:1) |
| `--line` | `#dfe3df` | Линии таблиц и разделители |
| `--accent` | `#2e6210` | **Кнопки, ссылки, мелкий акцентный текст** (7,3:1) |
| `--accent-bright` | `#448a16` | Только графика и элементы от 24 px (4,3:1 — ниже AA для текста) |
| `--dark` | `#111c14` | Якорные секции и футер |
| `--dark-ink` | `#eef2ec` | Текст на тёмном (14,8:1) |
| `--warn` | `#a8620a` | Пометки «предварительно» |

🔴 Мелкий текст и кнопки зелёным — **только `--accent`**. Яркий не проходит AA.

## Типографика

**Geist Sans**, числа `tabular-nums`. Geist Mono только там, где колонка цифр должна совпадать посимвольно.

| Уровень | Десктоп | Мобильный |
|---|---|---|
| display | 64 | 40 |
| h2 | 40 | 28 |
| h3 | 24 | 20 |
| body | 17 | 16 |
| small | 14 | 13 |
| num (число как графика) | 56 | 36 |

Разрыв между уровнями не меньше 1,5×.

## Материалы и правила

- Единый радиус **6 px**. Никаких «пилюль»
- **Границы вместо теней.** Тень допустима только под всплывающим слоем, не шире 6 px
- **Градиентов нет**
- Разделитель — линия, а не рамка вокруг блока
- Линия-акцент 2 px `--accent-bright` шириной 64 px под ключевым блоком
- Текстовая колонка максимум **40 em** — не `ch`: в кириллице `ch` шире среднего знака и лимиты не держат

## Движение

Осознанно минимальное: аудитория trust-first, движение читается как несерьёзность.

- **Один авторский момент:** переключатель «Зима / Лето» на схеме и живой отклик калькулятора на ползунок
- Всё остальное: смена состояния 150–250 мс, `ease-out`, без пружин
- ⛔️ Появление блоков при прокрутке · накрутка цифр · параллакс · автокарусели
- `prefers-reduced-motion` уважается

## Запрещено

Карточка внутри карточки · кикер над заголовком · цветная полоса слева у врезки · тень шире 6 px · градиент · скруглённая «пилюля» · иконка в круге · эмодзи вместо иконки · длинное тире в текстах · сетка из одинаковых карточек как структура страницы.

## Ручки impeccable

`DESIGN_VARIANCE 4 · MOTION_INTENSITY 2 · VISUAL_DENSITY 7`

Обоснование: аудитория проверяет числа, а не любуется; плотность данных на экран — сигнал компетентности; движение снижает доверие.
